version: '3.8'

services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile-test
    volumes:
      - .:/app
    command: pytest -s -v
    networks:
      - mynetwork
networks:
  mynetwork:

